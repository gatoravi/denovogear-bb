#include <RInside.h>     // for the embedded R via RInside
#include <stdio.h>
#include <vector>

int BBFit(int argc, char* argv[], std::vector<int> k1, std::vector<int> n1, RInside& R, double& alpha, double& beta);
